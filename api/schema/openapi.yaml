openapi: 3.1.0
info:
  title: CMCEN / RCMCE API
  version: 1.0.0
  description: >
    OpenAPI-style inventory for the Express routes mounted by server/server.js.
    Schemas are intentionally broad where handlers return Mongoose documents.
    Every /api endpoint is rate limited by source IP and may return 429 with
    Retry-After and RateLimit-* headers. Sensitive endpoints have stricter limits.
servers:
  - url: http://localhost:3000
    description: Local development (Eric)
  - url: http://localhost:3001
    description: Local development (Bray)
  - url: https://cmcen-staging.corebot.ca
    description: Staging
  - url: https://beta.cmcen-rcmce.ca
    description: Beta
security:
  - bearerAuth: []
tags:
  - name: Site
  - name: System
  - name: Auth
  - name: MFA
  - name: Analytics
  - name: Audit Log
  - name: Admin
  - name: Site Config
  - name: Uploads
  - name: Banners
  - name: Translations
  - name: Search
  - name: Pages
  - name: Events
  - name: News
  - name: Last Posts
  - name: Retirements
  - name: Certificates
paths:
  /api/certificate-requests:
    get:
      tags: [Certificates]
      summary: List certificate requests for fulfillment.
      description: Returns requests that still need printing or mailing by default. Supply a status or `status=all` to include completed requests.
      x-permission: canManageCertificateRequests
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [pending, ready_to_mail, mailed, printed, actionable, all]
      responses:
        "200":
          description: Certificate request worklist.
  /api/certificate-requests/count:
    get:
      tags: [Certificates]
      summary: Count actionable certificate requests.
      x-permission: canManageCertificateRequests
      responses:
        "200":
          description: Returns `pending`, `readyToMail`, and total `actionable` counts.
  /api/certificate-requests/{certificateRequestId}/status:
    patch:
      tags: [Certificates]
      summary: Advance certificate fulfillment.
      description: Confirm every requested certificate is printed to move from `pending` to `ready_to_mail`, then confirm mailing to move to `mailed`. The actor and timestamp are recorded in the audit log.
      x-permission: canManageCertificateRequests
      parameters:
        - $ref: "#/components/parameters/CertificateRequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [ready_to_mail, mailed]
                printedCertificateKeys:
                  type: array
                  description: Required for the `ready_to_mail` transition. Must contain `member` and every requested `family:{index}` key.
                  items:
                    type: string
      responses:
        "200":
          description: Certificate request fulfillment status updated.
  /robots.txt:
    get:
      tags: [Site]
      summary: Get crawler directives.
      security: []
      responses:
        "200":
          description: Robots.txt content with the absolute XML sitemap location.
          content:
            text/plain:
              schema:
                type: string
  /sitemap.xml:
    get:
      tags: [Site]
      summary: Get the crawler XML sitemap.
      security: []
      responses:
        "200":
          description: Public static pages and published public custom pages.
          content:
            application/xml:
              schema:
                type: string
  /site.webmanifest:
    get:
      tags: [Site]
      summary: Get web application metadata.
      security: []
      responses:
        "200":
          description: Web application manifest.
          content:
            application/manifest+json:
              schema:
                type: object
  /llms.txt:
    get:
      tags: [Site]
      summary: Get the AI access-policy notice.
      security: []
      responses:
        "200":
          description: Plain-text notice that does not grant AI systems permission to use site content.
          content:
            text/plain:
              schema:
                type: string
  /api/version:
    get:
      tags: [System]
      summary: Get running build version.
      security: []
      responses:
        "200":
          description: Build metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionResponse"
  /api/client-config/plausible:
    get:
      tags: [System]
      summary: Get public Plausible tracker configuration.
      description: "Returns `{ enabled: false }` unless both self-hosted Plausible environment settings are configured."
      security: []
      responses:
        "200":
          description: Public Plausible tracker configuration.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlausibleConfig"
  /api/data:
    get:
      tags: [System]
      summary: Public smoke-test endpoint.
      security: []
      responses:
        "200":
          $ref: "#/components/responses/Message"
  /api/protected_data:
    get:
      tags: [System]
      summary: Authenticated smoke-test endpoint.
      responses:
        "200":
          $ref: "#/components/responses/Message"
        "401":
          $ref: "#/components/responses/Error"
  /trade-options.js:
    get:
      tags: [System]
      summary: Get browser trade-options runtime.
      security: []
      responses:
        "200":
          description: JavaScript response.
  /api/content-options:
    get:
      tags: [System]
      summary: Get shared content option lists.
      security: []
      responses:
        "200":
          description: Content option data.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /translations.json:
    get:
      tags: [Translations]
      summary: Get public translation dictionary.
      security: []
      responses:
        "200":
          description: Translation dictionary.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /translations.js:
    get:
      tags: [Translations]
      summary: Get browser translation runtime.
      security: []
      responses:
        "200":
          description: JavaScript response.
  /api/translations:
    get:
      tags: [Translations]
      summary: List translations for admin editor.
      x-permission: canManageTranslations
      responses:
        "200":
          description: Translation rows.
  /api/translations/{key}:
    patch:
      tags: [Translations]
      summary: Update a translation key.
      x-permission: canManageTranslations
      parameters:
        - $ref: "#/components/parameters/TranslationKey"
      requestBody:
        $ref: "#/components/requestBodies/TranslationUpdate"
      responses:
        "200":
          description: Updated translation.
  /api/ghost/request:
    post:
      tags: [Auth]
      summary: Request ghost-account claim.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Claim request result.
  /api/ghost/confirm:
    post:
      tags: [Auth]
      summary: Confirm ghost-account claim and issue a session only after cookie consent.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Claim confirmation result.
  /api/register:
    post:
      tags: [Auth]
      summary: Register a user account; invitation activation issues a session only after cookie consent.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/AuthRegister"
      responses:
        "201":
          description: Registered user and token/verification state.
        "400":
          $ref: "#/components/responses/Error"
  /api/invitations/activate:
    get:
      tags: [Auth]
      summary: Load a valid invitation's prefilled identity.
      security: []
      parameters:
        - name: token
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Invited name and email.
        "400":
          $ref: "#/components/responses/Error"
  /api/login:
    post:
      tags: [Auth]
      summary: Login with username and password after confirming the required session cookie.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/Login"
      responses:
        "200":
          description: Login result; may require MFA.
  /api/session/refresh:
    post:
      tags: [Auth]
      summary: Exchange a valid refresh-token cookie for a new access token.
      security: []
      responses:
        "200":
          description: Refreshed access token.
        "401":
          $ref: "#/components/responses/Error"
  /api/session/logout:
    post:
      tags: [Auth]
      summary: Revoke the refresh session and clear its cookie.
      security: []
      responses:
        "204":
          description: Session revoked.
  /api/email-verification/confirm:
    post:
      tags: [Auth]
      summary: Confirm email verification token and issue a session only after cookie consent.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Verification result.
  /api/password-reset/request:
    post:
      tags: [Auth]
      summary: Request password reset.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Reset request result.
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/password-reset/confirm:
    post:
      tags: [Auth]
      summary: Confirm password reset.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Reset confirmation result.
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/me:
    get:
      tags: [Auth]
      summary: Get current user.
      responses:
        "200":
          description: Current user, permissions, and notifications.
  /api/notifications:
    get:
      tags: [Auth]
      summary: Get user notifications.
      responses:
        "200":
          description: Notification summary.
  /api/subscriptions/weekly-brief:
    put:
      tags: [Auth]
      summary: Set the current user's weekly email brief subscription.
      description: "Express opt-in requires `subscribed: true` and `expressConsent: true`. Withdrawal is immediate and is recorded in the audit log."
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Updated current user profile and subscription state.
        "400":
          $ref: "#/components/responses/Error"
  /api/subscriptions/weekly-brief/unsubscribe:
    get:
      tags: [Auth]
      security: []
      summary: Unsubscribe a recipient using an opaque email token.
      parameters:
        - in: query
          name: token
          required: true
          schema:
            type: string
      responses:
        "200":
          description: HTML confirmation that the weekly brief has been unsubscribed.
        "404":
          description: The token is invalid or expired.
    post:
      tags: [Auth]
      security: []
      summary: Process an RFC 8058 one-click unsubscribe request.
      parameters:
        - in: query
          name: token
          required: true
          schema:
            type: string
      responses:
        "200":
          description: HTML confirmation that the weekly brief has been unsubscribed.
        "404":
          description: The token is invalid or expired.
  /api/subscriptions/news-announcements:
    put:
      tags: [Auth]
      summary: Set the current user's news-announcement subscription.
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Updated profile and subscription state.
  /api/admin/subscriptions:
    get:
      tags: [Admin]
      summary: List email subscribers and sent newsletter history.
      x-permission: canManageSubscriptions
      responses:
        "200":
          description: Subscriber list and newsletter history.
  /api/admin/subscriptions/export.csv:
    get:
      tags: [Admin]
      summary: Export subscribed users to CSV.
      x-permission: canManageSubscriptions
      responses:
        "200":
          description: Subscription CSV export.
  /api/admin/subscriptions/news-blasts:
    post:
      tags: [Admin]
      summary: Send an announcement to express news-announcement subscribers.
      x-permission: canManageSubscriptions
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "201":
          description: News blast delivery result.
  /api/ghost/upgrade:
    post:
      tags: [Auth]
      summary: Upgrade or merge ghost account.
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Upgrade result.
  /api/profile:
    delete:
      tags: [Auth]
      summary: Delete the current account after a current TOTP or passkey MFA confirmation.
      x-permission: canDeleteOwnAccount
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Account deleted and content anonymized.
        "403":
          $ref: "#/components/responses/Error"
    patch:
      tags: [Auth]
      summary: Update current user profile.
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Updated user profile.
  /api/contributor-check:
    get:
      tags: [Auth]
      summary: Confirm contributor access.
      x-permission: canCreateDrafts
      responses:
        "200":
          $ref: "#/components/responses/Message"
  /api/admin-check:
    get:
      tags: [Auth]
      summary: Confirm admin access.
      responses:
        "200":
          $ref: "#/components/responses/Message"
  /api/mfa/webauthn/register/options:
    post:
      tags: [MFA]
      summary: Generate WebAuthn registration options.
      responses:
        "200":
          description: WebAuthn registration options.
  /api/mfa/webauthn/register/verify:
    post:
      tags: [MFA]
      summary: Verify WebAuthn registration.
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Stored credential result.
  /api/mfa/webauthn/authenticate/options:
    post:
      tags: [MFA]
      summary: Generate WebAuthn authentication options.
      parameters:
        - $ref: "#/components/parameters/TempTokenQuery"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: WebAuthn authentication options.
  /api/mfa/webauthn/authenticate/verify:
    post:
      tags: [MFA]
      summary: Verify WebAuthn authentication.
      parameters:
        - $ref: "#/components/parameters/TempTokenQuery"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Authentication result; may include JWT.
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/mfa/totp/setup:
    post:
      tags: [MFA]
      summary: Initialize TOTP setup with a server-assigned authenticator name.
      responses:
        "200":
          description: TOTP secret, otpauth URL, and QR data.
  /api/mfa/totp/status:
    get:
      tags: [MFA]
      summary: Get TOTP status.
      responses:
        "200":
          description: TOTP status.
  /api/mfa/totp/qrcode:
    get:
      tags: [MFA]
      summary: Get TOTP QR code.
      responses:
        "200":
          description: TOTP QR code data.
  /api/mfa/totp/verify:
    post:
      tags: [MFA]
      summary: Verify TOTP code.
      parameters:
        - $ref: "#/components/parameters/TempTokenQuery"
      requestBody:
        $ref: "#/components/requestBodies/TotpVerify"
      responses:
        "200":
          description: Verification result; may include JWT.
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/mfa/totp:
    delete:
      tags: [MFA]
      summary: Disable active TOTP or cancel a pending setup.
      responses:
        "200":
          description: Disable result.
  /api/mfa/webauthn/credentials:
    get:
      tags: [MFA]
      summary: List passkey credentials.
      responses:
        "200":
          description: Credential list.
  /api/mfa/webauthn/credentials/{credentialID}:
    patch:
      tags: [MFA]
      summary: Rename passkey credential.
      parameters:
        - $ref: "#/components/parameters/CredentialID"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Rename result.
    delete:
      tags: [MFA]
      summary: Delete passkey credential.
      parameters:
        - $ref: "#/components/parameters/CredentialID"
      responses:
        "200":
          description: Delete result.
  /api/mfa/webauthn/cleanup:
    post:
      tags: [MFA]
      summary: Cleanup invalid passkey records.
      responses:
        "200":
          description: Updated credential list.
  /api/analytics:
    get:
      tags: [Analytics]
      summary: Get analytics summary.
      description: Returns visit totals and unique visitor totals. Registered visitors are deduplicated by user id; guest visitors are deduplicated by stored IP address and user agent. Role counts represent unique visitors by role, not raw page refreshes.
      x-permission: canViewAnalytics
      parameters:
        - name: range
          in: query
          schema:
            type: string
            enum: [7d, 30d, 90d, all]
            default: 30d
      responses:
        "200":
          description: Analytics summary.
  /api/analytics/visit:
    post:
      tags: [Analytics]
      summary: Record analytics visit.
      security: []
      requestBody:
        $ref: "#/components/requestBodies/AnalyticsVisit"
      responses:
        "204":
          description: Visit accepted.
  /api/audit-logs:
    get:
      tags: [Audit Log]
      summary: List audit log entries.
      x-permission: canViewAuditLog
      parameters:
        - name: action
          in: query
          schema: { type: string }
        - name: targetType
          in: query
          schema: { type: string }
        - name: user
          in: query
          schema: { type: string }
        - name: startDate
          in: query
          schema: { type: string, format: date }
        - name: endDate
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Audit logs.
  /api/audit-logs/export.csv:
    get:
      tags: [Audit Log]
      summary: Export audit log entries as CSV.
      x-permission: canViewAuditLog
      parameters:
        - name: action
          in: query
          schema: { type: string }
        - name: targetType
          in: query
          schema: { type: string }
        - name: user
          in: query
          schema: { type: string }
        - name: startDate
          in: query
          schema: { type: string, format: date }
        - name: endDate
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Audit log CSV export.
          content:
            text/csv:
              schema:
                type: string
  /api/admin/review-counts:
    get:
      tags: [Admin]
      summary: Get pending review counts.
      x-permission: canReviewAndPublish
      responses:
        "200":
          description: Review counts.
  /api/admin/roles:
    get:
      tags: [Admin]
      summary: List custom roles.
      x-permission: canManageRoles
      responses:
        "200":
          description: Roles and permission catalog.
    post:
      tags: [Admin]
      summary: Create custom role.
      x-permission: canManageRoles
      requestBody:
        $ref: "#/components/requestBodies/Role"
      responses:
        "201":
          description: Created role.
  /api/admin/roles/{roleId}:
    patch:
      tags: [Admin]
      summary: Update custom role.
      x-permission: canManageRoles
      parameters:
        - $ref: "#/components/parameters/RoleId"
      requestBody:
        $ref: "#/components/requestBodies/Role"
      responses:
        "200":
          description: Updated role.
    delete:
      tags: [Admin]
      summary: Delete custom role.
      x-permission: canManageRoles
      parameters:
        - $ref: "#/components/parameters/RoleId"
      responses:
        "200":
          description: Delete result.
  /api/admin/media:
    get:
      tags: [Admin, Uploads]
      summary: List media assets.
      description: Returns media assets with linked event, retirement, and Last Post usage metadata. Supports type filtering and filename/image-name search.
      x-permission: canViewMediaLibrary
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: cursor
          in: query
          schema: { type: string }
        - name: sort
          in: query
          schema:
            type: string
            enum: [newest, oldest, name, size, orphaned]
        - name: type
          in: query
          schema:
            type: string
            enum:
              [
                all,
                retirement,
                last-post,
                event,
                page,
                upload,
                migration,
                unattached,
              ]
        - name: search
          in: query
          description: Case-insensitive match against filename, image name, key, CDN slug, or upload label.
          schema: { type: string, maxLength: 120 }
      responses:
        "200":
          description: Media assets.
  /api/admin/media/bulk-delete:
    post:
      tags: [Admin, Uploads]
      summary: Delete selected unattached media assets.
      description: Deletes selected media assets by key. Attached media assets are skipped and returned in the response.
      x-permission: canDeleteMedia
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Bulk delete result.
  /api/admin/media/{key}:
    delete:
      tags: [Admin, Uploads]
      summary: Delete unattached media asset.
      x-permission: canDeleteMedia
      parameters:
        - $ref: "#/components/parameters/ObjectKey"
      responses:
        "200":
          description: Delete result.
        "409":
          $ref: "#/components/responses/Error"
  /api/admin/users:
    post:
      tags: [Admin]
      summary: Provision an invited user account.
      description: Creates an invited account and emails a seven-day activation link. The recipient sets their own password and completes their profile. An optional `message` replaces the standard account-creation message in the email and is retained for a resend. Developer accounts cannot be provisioned through this endpoint, and only a developer may choose the `internal_beta` role.
      x-permission: canProvisionUsers
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [firstName, lastName, email, role]
              properties:
                firstName:
                  type: string
                  minLength: 1
                  maxLength: 80
                lastName:
                  type: string
                  minLength: 1
                  maxLength: 80
                email:
                  type: string
                  format: email
                message:
                  type: string
                  maxLength: 2000
                  description: Optional plain-text message that replaces the standard account-creation message in the invitation email.
                role:
                  type: string
                  enum:
                    [
                      subscriber,
                      internal_beta,
                      contributor,
                      author,
                      editor,
                      administrator,
                    ]
      responses:
        "201":
          description: Invitation sent.
        "400":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"
    get:
      tags: [Admin]
      summary: List users.
      description: Returns lightweight user rows for the admin user manager. Use the user detail endpoint to load post summaries and editable detail for a selected user.
      x-permission: canReadUsers
      parameters:
        - name: query
          in: query
          schema: { type: string }
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Users.
  /api/admin/users/{userId}:
    delete:
      tags: [Admin]
      summary: Delete a user account after a current TOTP or passkey MFA confirmation.
      x-permission: canDeleteAnyUser
      parameters:
        - $ref: "#/components/parameters/UserId"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Account deleted with the selected content disposition.
        "403":
          $ref: "#/components/responses/Error"
    get:
      tags: [Admin]
      summary: Get user detail.
      x-permission: canReadUsers
      parameters:
        - $ref: "#/components/parameters/UserId"
      responses:
        "200":
          description: User detail.
    patch:
      tags: [Admin]
      summary: Update user admin assignments.
      x-permission: canManageUsers
      parameters:
        - $ref: "#/components/parameters/UserId"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Updated user.
  /api/admin/users/{userId}/role:
    patch:
      tags: [Admin]
      summary: Update built-in role.
      description: Assigns a built-in role. `internal_beta` has the same site permissions as a subscriber and identifies members approved for internal beta software; only a developer may assign or remove it.
      x-permission: canManageUsers
      parameters:
        - $ref: "#/components/parameters/UserId"
      requestBody:
        $ref: "#/components/requestBodies/BuiltInRole"
      responses:
        "200":
          description: Updated role.
  /api/admin/users/{userId}/invitation/resend:
    post:
      tags: [Admin]
      summary: Resend an invited user's activation email.
      description: Rotates the activation token, extends the invitation by seven days, and retries email delivery. Resending an `internal_beta` invitation requires a developer.
      x-permission: canProvisionUsers
      parameters:
        - $ref: "#/components/parameters/UserId"
      responses:
        "200":
          description: Invitation resent.
        "400":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
        "502":
          $ref: "#/components/responses/Error"
  /api/admin/users/{userId}/developer:
    patch:
      tags: [Admin]
      summary: Promote an administrator to developer.
      description: Requires canManageUsers plus an authenticated current user whose built-in role is developer. Only target users with the administrator role can be promoted; subscribers and other non-administrator accounts cannot be promoted directly.
      x-permission: canManageUsers
      parameters:
        - $ref: "#/components/parameters/UserId"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Promotion result.
  /api/admin/last-posts/{lastPostId}:
    delete:
      tags: [Admin, LastPost]
      summary: Delete a Last Post notice.
      description: Allowed for administrators/developers or the original submitter with own-content deletion permission. Administrator deletion removes an attached image and its generated variants when no other content still references that asset.
      parameters:
        - name: lastPostId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Last Post notice deleted.
        "403":
          $ref: "#/components/responses/Error"
  /api/admin/events/{eventId}:
    delete:
      tags: [Admin, Events]
      summary: Delete any event.
      description: Administrator deletion removes an attached image and its generated variants when no other content still references that asset.
      x-permission: canDeleteContent
      parameters:
        - $ref: "#/components/parameters/EventId"
      responses:
        "200":
          description: Delete result.
  /api/admin/retirement-messages/{messageId}:
    delete:
      tags: [Admin, Retirements]
      summary: Delete any retirement message.
      description: Administrator deletion removes an attached image and its generated variants when no other content still references that asset.
      x-permission: canDeleteContent
      parameters:
        - $ref: "#/components/parameters/MessageId"
      responses:
        "200":
          description: Delete result.
  /api/admin/retirement-comments/{commentId}:
    delete:
      tags: [Admin, Retirements]
      summary: Delete any retirement comment.
      x-permission: canDeleteContent
      parameters:
        - $ref: "#/components/parameters/CommentId"
      responses:
        "200":
          description: Delete result.
  /api/admin/site-config/access:
    post:
      tags: [Site Config]
      summary: Log site config access request.
      x-permission: canAccessSiteConfig
      responses:
        "200":
          description: Access recorded.
  /api/admin/site-config/verify:
    post:
      tags: [Site Config]
      summary: Verify site-config token.
      x-permission: canAccessSiteConfig
      requestBody:
        $ref: "#/components/requestBodies/SiteConfigToken"
      responses:
        "200":
          description: Verification result.
  /api/admin/site-config:
    get:
      tags: [Site Config]
      summary: Read available site operations.
      x-permission: canAccessSiteConfig
      responses:
        "200":
          description: Available protected operations.
  /api/admin/site-config/analytics:
    delete:
      tags: [Site Config, Analytics]
      summary: Purge analytics history.
      description: Token-gated protected maintenance action.
      x-permission: canManageSiteConfig
      responses:
        "200":
          description: Purge result.
  /api/upload:
    post:
      tags: [Uploads]
      summary: Upload and sanitize image through server.
      description: Every stored image is validated and re-encoded as metadata-free WebP before it is written to object storage. Direct-to-storage uploads are not supported. Retirement and Last Post uploads may request a 4:3 display derivative while preserving the original image.
      x-permission: canUploadMedia
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [image]
              properties:
                image:
                  type: string
                  format: binary
                uploadSource:
                  type: string
                  enum:
                    [
                      retirementMessage,
                      lastPostMessage,
                      event,
                      mediaManager,
                      pageBuilder,
                      migration,
                      directUpload,
                      legacyStorage,
                      unknown,
                    ]
                  description: UI or workflow that initiated the upload.
                uploadContext:
                  type: string
                  description: Human-readable context such as retirement-message, last-post, event, or media-manager.
                sourceId:
                  type: string
                  description: Source content id when known.
                sourceModel:
                  type: string
                  description: Source model or content type when known.
                sourceField:
                  type: string
                  description: Source field that stores the media URL, such as photoUrl, imageUrl, or imagePath.
                sourceUrl:
                  type: string
                  description: Link back to the source content when known.
                sourceSlug:
                  type: string
                  description: Source slug when known.
                sourceName:
                  type: string
                  description: Name inferred from the source content, such as retiree, deceased member, or event title.
                displayAspectRatio:
                  type: string
                  enum: ["4:3"]
                  description: Creates a cropped display derivative for retirement or Last Post uploads.
                displayCropX:
                  type: number
                  minimum: 0
                  maximum: 1
                  default: 0.5
                  description: Horizontal focal position in the requested display derivative.
                displayCropY:
                  type: number
                  minimum: 0
                  maximum: 1
                  default: 0.5
                  description: Vertical focal position in the requested display derivative.
                cdnSlug:
                  type: string
                  pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
                  maxLength: 80
                  description: Optional unique CDN path slug. Creates objects under images/{cdnSlug}/.
      responses:
        "201":
          description: Upload result with variants.
  /api/upload-url:
    post:
      tags: [Uploads]
      summary: Retired direct-upload endpoint.
      description: Always returns 410 because direct-to-storage uploads bypass mandatory metadata removal. Use POST /api/upload instead.
      x-permission: canUploadMedia
      responses:
        "410":
          description: Direct uploads are permanently disabled.
  /api/image/{key}:
    get:
      tags: [Uploads]
      summary: Create signed image download URL.
      x-permission: canViewMediaLibrary
      parameters:
        - $ref: "#/components/parameters/ObjectKey"
      responses:
        "200":
          description: Signed URL.
  /api/timers/active:
    get:
      tags: [Banners]
      summary: List active public banners.
      security: []
      parameters:
        - name: scope
          in: query
          schema:
            type: string
            enum: [global, home]
            default: global
      responses:
        "200":
          description: Active banners.
  /api/admin/timers:
    get:
      tags: [Banners]
      summary: List all banners.
      x-permission: canManageTimers
      responses:
        "200":
          description: Banners.
    post:
      tags: [Banners]
      summary: Create banner.
      x-permission: canManageTimers
      requestBody:
        $ref: "#/components/requestBodies/Banner"
      responses:
        "201":
          description: Created banner.
  /api/admin/timers/{timerId}:
    patch:
      tags: [Banners]
      summary: Update banner.
      x-permission: canManageTimers
      parameters:
        - $ref: "#/components/parameters/TimerId"
      requestBody:
        $ref: "#/components/requestBodies/Banner"
      responses:
        "200":
          description: Updated banner.
    delete:
      tags: [Banners]
      summary: Delete banner.
      x-permission: canManageTimers
      parameters:
        - $ref: "#/components/parameters/TimerId"
      responses:
        "200":
          description: Delete result.
  /api/search:
    get:
      tags: [Search]
      summary: Search published news stories and non-Home public content with singular/plural-aware title ranking.
      security: []
      parameters:
        - name: q
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Search results.
          content:
            application/json:
              schema:
                type: object
                required: [query, total, results]
                properties:
                  query:
                    type: string
                  total:
                    type: integer
                  results:
                    type: array
                    items:
                      type: object
                      required: [type, sourceId, title, summary, url]
                      properties:
                        type:
                          type: string
                          enum:
                            [
                              event,
                              retirement-message,
                              last-post-message,
                              news-story,
                              page,
                            ]
                        sourceId:
                          type: string
                        title:
                          type: string
                        summary:
                          type: string
                        url:
                          type: string
                          description: Canonical relative URL for the public result destination.
                        date:
                          type: [string, "null"]
                          format: date-time
  /pages/{slug}:
    get:
      tags: [Pages]
      summary: Serve page shell.
      security: []
      parameters:
        - $ref: "#/components/parameters/Slug"
      responses:
        "200":
          description: HTML page shell.
  /api/navigation:
    get:
      tags: [Pages]
      summary: Get dynamic navigation.
      security: []
      responses:
        "200":
          description: Navigation items.
  /api/sitemap:
    get:
      tags: [Pages]
      summary: Get generated sitemap sections and links.
      security: []
      responses:
        "200":
          description: Generated sitemap from public HTML files and published pages.
  /api/pages/{slug}:
    get:
      tags: [Pages]
      summary: Get public page content.
      security: []
      parameters:
        - $ref: "#/components/parameters/Slug"
      responses:
        "200":
          description: Page content.
  /api/admin/pages/{pageId}/preview:
    get:
      tags: [Pages]
      summary: Preview page.
      x-permission: canManagePages
      parameters:
        - $ref: "#/components/parameters/PageId"
      responses:
        "200":
          description: Page preview.
  /api/admin/pages:
    get:
      tags: [Pages]
      summary: List pages for admin.
      x-permission: canManagePages
      responses:
        "200":
          description: Admin page list.
    post:
      tags: [Pages]
      summary: Create page.
      x-permission: canManagePages
      requestBody:
        $ref: "#/components/requestBodies/Page"
      responses:
        "201":
          description: Created page.
  /api/admin/pages/media:
    get:
      tags: [Pages, Uploads]
      summary: List page-editor media assets.
      x-permission: canManagePages
      responses:
        "200":
          description: Media picker data.
  /api/admin/pages/{pageId}:
    get:
      tags: [Pages]
      summary: Get full admin page payload.
      x-permission: canManagePages
      parameters:
        - $ref: "#/components/parameters/PageId"
      responses:
        "200":
          description: Page payload.
    patch:
      tags: [Pages]
      summary: Update page.
      x-permission: canManagePages
      parameters:
        - $ref: "#/components/parameters/PageId"
      requestBody:
        $ref: "#/components/requestBodies/Page"
      responses:
        "200":
          description: Updated page.
    delete:
      tags: [Pages]
      summary: Delete page.
      x-permission: canManagePages
      parameters:
        - $ref: "#/components/parameters/PageId"
      responses:
        "200":
          description: Delete result.
  /api/admin/pages/{pageId}/status:
    patch:
      tags: [Pages]
      summary: Update page status.
      x-permission: canManagePages
      parameters:
        - $ref: "#/components/parameters/PageId"
      requestBody:
        $ref: "#/components/requestBodies/PageStatus"
      responses:
        "200":
          description: Updated page status.
  /api/admin/navigation-items:
    post:
      tags: [Pages]
      summary: Create navigation item.
      x-permission: canManageNavigation
      requestBody:
        $ref: "#/components/requestBodies/NavigationItem"
      responses:
        "201":
          description: Created navigation item.
  /api/admin/navigation-items/{itemId}:
    patch:
      tags: [Pages]
      summary: Update navigation item.
      x-permission: canManageNavigation
      parameters:
        - $ref: "#/components/parameters/ItemId"
      requestBody:
        $ref: "#/components/requestBodies/NavigationItem"
      responses:
        "200":
          description: Updated navigation item.
    delete:
      tags: [Pages]
      summary: Delete navigation item.
      x-permission: canManageNavigation
      parameters:
        - $ref: "#/components/parameters/ItemId"
      responses:
        "200":
          description: Delete result.
  /api/events:
    get:
      tags: [Events]
      summary: List published events.
      security: []
      parameters:
        - name: from
          in: query
          required: false
          description: >
            Inclusive range start as YYYY-MM-DD. Must be supplied with `to`;
            range queries include events that overlap the dates.
          schema:
            type: string
            format: date
            pattern: "^\\d{4}-\\d{2}-\\d{2}$"
        - name: to
          in: query
          required: false
          description: >
            Inclusive range end as YYYY-MM-DD. Must be supplied with `from`;
            requested ranges cannot exceed 370 days.
          schema:
            type: string
            format: date
            pattern: "^\\d{4}-\\d{2}-\\d{2}$"
        - name: eventType
          in: query
          required: false
          description: Only return events with this event type.
          schema:
            type: string
            enum: [conference, mess-function, ceremony, training, social, other]
        - name: organizingEntity
          in: query
          required: false
          description: Only return events organized by this entity.
          schema:
            type: string
            enum: [branch, association, foundation, museum]
        - name: provinceRegion
          in: query
          required: false
          description: Only return events in this province or region.
          schema:
            type: string
            enum:
              [
                AB,
                BC,
                MB,
                NB,
                NL,
                NS,
                NT,
                NU,
                ON,
                PE,
                QC,
                SK,
                YT,
                International,
              ]
      responses:
        "200":
          description: >
            Published upcoming events when no range is supplied, or published
            events overlapping the requested range.
        "400":
          $ref: "#/components/responses/Error"
    post:
      tags: [Events]
      summary: Submit event.
      description: Submitter details are copied from the authenticated user's profile. Descriptions and registration instructions are limited to 10,000 characters per language.
      x-permission: canCreateDrafts
      requestBody:
        $ref: "#/components/requestBodies/Event"
      responses:
        "201":
          description: Created event.
  /api/events/review:
    get:
      tags: [Events]
      summary: List event review queue.
      x-permission: canReviewAndPublish
      responses:
        "200":
          description: Review events.
  /api/events/mine:
    get:
      tags: [Events]
      summary: List current user's events.
      responses:
        "200":
          description: User events.
  /api/events/{id}:
    get:
      tags: [Events]
      summary: Get published event.
      security: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Event.
    patch:
      tags: [Events]
      summary: Update event.
      description: Updates event content while preserving the original submitter record. Descriptions and registration instructions are limited to 10,000 characters per language.
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        $ref: "#/components/requestBodies/Event"
      responses:
        "200":
          description: Updated event.
  /api/events/{id}/edit:
    get:
      tags: [Events]
      summary: Get event edit payload.
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Event edit payload.
  /api/events/{eventId}/review-content:
    patch:
      tags: [Events]
      summary: Update one language of a pending event during review.
      description: Updates title, location, description, and registration text for one language without changing the pending review status.
      x-permission: canReviewAndPublish
      parameters:
        - $ref: "#/components/parameters/EventId"
      requestBody:
        $ref: "#/components/requestBodies/EventReviewContent"
      responses:
        "200":
          description: Updated event review content.
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"
  /api/events/{eventId}/review:
    patch:
      tags: [Events]
      summary: Publish or reject event.
      x-permission: canReviewAndPublish
      parameters:
        - $ref: "#/components/parameters/EventId"
      requestBody:
        $ref: "#/components/requestBodies/ReviewDecision"
      responses:
        "200":
          description: Review result.
  /api/news:
    get:
      tags: [News]
      summary: List published bilingual news stories.
      security: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 48
      responses:
        "200":
          description: Published news stories.
    post:
      tags: [News]
      summary: Create a bilingual news story.
      x-permission: canManageNews
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewsArticleInput"
      responses:
        "201":
          description: Created news story.
  /api/news/feed:
    get:
      tags: [News]
      summary: List newest published news stories and Last Post notices together.
      security: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 24
      responses:
        "200":
          description: Combined public news feed.
  /api/news/manage:
    get:
      tags: [News]
      summary: List all news stories, including drafts, for editorial management.
      x-permission: canManageNews
      responses:
        "200":
          description: Managed news stories.
  /api/news/{articleId}:
    get:
      tags: [News]
      summary: Get one published bilingual news story for its full article page.
      security: []
      parameters:
        - $ref: "#/components/parameters/ArticleId"
      responses:
        "200":
          description: Published news story.
        "404":
          $ref: "#/components/responses/Error"
    patch:
      tags: [News]
      summary: Update a bilingual news story or its publication state.
      x-permission: canManageNews
      parameters:
        - $ref: "#/components/parameters/ArticleId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewsArticleInput"
      responses:
        "200":
          description: Updated news story.
    delete:
      tags: [News]
      summary: Delete a news story.
      x-permission: canManageNews
      parameters:
        - $ref: "#/components/parameters/ArticleId"
      responses:
        "200":
          description: Deleted news story.
  /api/last-posts:
    get:
      tags: [Last Posts]
      summary: List published Last Post notices.
      security: []
      responses:
        "200":
          description: Paginated Last Post notices.
    post:
      tags: [Last Posts]
      summary: Submit a Last Post notice.
      description: Chain-of-command consent is required. The notice enters the review queue by default. Set `publishNow` to `true` only when the authenticated user has `canReviewAndPublish`. Notice text is limited to 10,000 characters.
      x-permission: canCreateDrafts
      requestBody:
        $ref: "#/components/requestBodies/LastPostSubmission"
      responses:
        "201":
          description: Created Last Post notice.
  /api/last-posts/review:
    get:
      tags: [Last Posts]
      summary: List pending Last Post notices.
      x-permission: canReviewAndPublish
      responses:
        "200":
          description: Last Post review queue.
  /api/last-posts/{messageId}:
    get:
      tags: [Last Posts]
      summary: Get a published Last Post notice.
      security: []
      parameters:
        - $ref: "#/components/parameters/MessageId"
      responses:
        "200":
          description: Last Post notice.
        "404":
          $ref: "#/components/responses/Error"
  /api/last-posts/{messageId}/review-content:
    patch:
      tags: [Last Posts]
      summary: Update one language of a pending Last Post notice during review.
      description: Updates one Last Post translation without changing the pending review status.
      x-permission: canReviewAndPublish
      parameters:
        - $ref: "#/components/parameters/MessageId"
      requestBody:
        $ref: "#/components/requestBodies/LastPostReviewContent"
      responses:
        "200":
          description: Updated Last Post review content.
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
  /api/last-posts/{messageId}/review:
    patch:
      tags: [Last Posts]
      summary: Publish or reject a Last Post notice.
      x-permission: canReviewAndPublish
      parameters:
        - $ref: "#/components/parameters/MessageId"
      requestBody:
        $ref: "#/components/requestBodies/ReviewDecision"
      responses:
        "200":
          description: Review result.
  /api/retirement-messages:
    post:
      tags: [Retirements]
      summary: Submit retirement message.
      description: Submitter name, email, and unit are copied from the authenticated profile; the request supplies only `submitter.relationship`. An optional complete `certificateRequest` also creates a separate pending certificate-request record associated with the retirement message. When `MAIL_TO_BRANCH` is configured, the server emails the normalized submission and canonical public photo URL to the internal branch mailbox for Power Automate processing. The submission enters the review queue by default. Set `publishNow` to `true` only when the authenticated user has `canBypassReviewStages`.
      x-permission: canSubmitRetirementMessages
      requestBody:
        $ref: "#/components/requestBodies/RetirementMessage"
      responses:
        "201":
          description: Created retirement message.
    get:
      tags: [Retirements]
      summary: List published retirement messages.
      security: []
      responses:
        "200":
          description: Retirement messages.
  /api/retirement-messages/review:
    get:
      tags: [Retirements]
      summary: List retirement-message review queue.
      x-permission: canReviewAndPublish
      responses:
        "200":
          description: Review messages.
  /api/retirement-messages/comments/review:
    get:
      tags: [Retirements]
      summary: List retirement-comment review queue.
      x-permission: canReviewAndPublish
      responses:
        "200":
          description: Review comments.
  /api/retirement-messages/comments/{commentId}/review:
    patch:
      tags: [Retirements]
      summary: Publish or reject retirement comment.
      x-permission: canReviewAndPublish
      parameters:
        - $ref: "#/components/parameters/CommentId"
      requestBody:
        $ref: "#/components/requestBodies/ReviewDecision"
      responses:
        "200":
          description: Review result.
  /api/retirement-messages/comments/{commentId}/edit:
    get:
      tags: [Retirements]
      summary: Get retirement comment edit payload.
      parameters:
        - $ref: "#/components/parameters/CommentId"
      responses:
        "200":
          description: Comment edit payload.
  /api/retirement-messages/comments/{commentId}:
    patch:
      tags: [Retirements]
      summary: Update retirement comment.
      parameters:
        - $ref: "#/components/parameters/CommentId"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "200":
          description: Updated comment.
  /api/retirement-messages/{messageId}/edit:
    get:
      tags: [Retirements]
      summary: Get retirement message edit payload.
      parameters:
        - $ref: "#/components/parameters/MessageId"
      responses:
        "200":
          description: Message edit payload.
  /api/retirement-messages/{messageId}:
    get:
      tags: [Retirements]
      summary: Get published retirement message.
      security: []
      parameters:
        - $ref: "#/components/parameters/MessageId"
      responses:
        "200":
          description: Retirement message.
    patch:
      tags: [Retirements]
      summary: Update retirement message.
      description: Updates retirement content while preserving the original submitter contact record; missing legacy contact fields are filled from the authenticated profile, and the request may update `submitter.relationship`. An optional complete `certificateRequest` creates a separate pending certificate-request record associated with the retirement message.
      parameters:
        - $ref: "#/components/parameters/MessageId"
      requestBody:
        $ref: "#/components/requestBodies/RetirementMessage"
      responses:
        "200":
          description: Updated retirement message.
  /api/retirement-messages/{messageId}/comments:
    get:
      tags: [Retirements]
      summary: List published comments for message.
      security: []
      parameters:
        - $ref: "#/components/parameters/MessageId"
      responses:
        "200":
          description: Comments.
    post:
      tags: [Retirements]
      summary: Create retirement comment.
      parameters:
        - $ref: "#/components/parameters/MessageId"
      requestBody:
        $ref: "#/components/requestBodies/GenericJson"
      responses:
        "201":
          description: Created comment.
  /api/retirement-messages/{messageId}/review-content:
    patch:
      tags: [Retirements]
      summary: Update one language of a pending retirement message during review.
      description: Updates one retirement message translation without changing the pending review status. Each saved message must contain at least 100 characters.
      x-permission: canReviewAndPublish
      parameters:
        - $ref: "#/components/parameters/MessageId"
      requestBody:
        $ref: "#/components/requestBodies/RetirementReviewContent"
      responses:
        "200":
          description: Updated retirement review content.
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"
  /api/retirement-messages/{messageId}/review:
    patch:
      tags: [Retirements]
      summary: Publish or reject retirement message.
      x-permission: canReviewAndPublish
      parameters:
        - $ref: "#/components/parameters/MessageId"
      requestBody:
        $ref: "#/components/requestBodies/ReviewDecision"
      responses:
        "200":
          description: Review result.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    Id:
      name: id
      in: path
      required: true
      schema: { type: string }
    ArticleId:
      name: articleId
      in: path
      required: true
      schema: { type: string }
    EventId:
      name: eventId
      in: path
      required: true
      schema: { type: string }
    MessageId:
      name: messageId
      in: path
      required: true
      schema: { type: string }
    CommentId:
      name: commentId
      in: path
      required: true
      schema: { type: string }
    CertificateRequestId:
      name: certificateRequestId
      in: path
      required: true
      schema: { type: string }
    UserId:
      name: userId
      in: path
      required: true
      schema: { type: string }
    RoleId:
      name: roleId
      in: path
      required: true
      schema: { type: string }
    PageId:
      name: pageId
      in: path
      required: true
      schema: { type: string }
    ItemId:
      name: itemId
      in: path
      required: true
      schema: { type: string }
    TimerId:
      name: timerId
      in: path
      required: true
      schema: { type: string }
    CredentialID:
      name: credentialID
      in: path
      required: true
      schema: { type: string }
    ObjectKey:
      name: key
      in: path
      required: true
      schema: { type: string }
    Slug:
      name: slug
      in: path
      required: true
      schema: { type: string }
    TranslationKey:
      name: key
      in: path
      required: true
      schema: { type: string }
    TempTokenQuery:
      name: tempToken
      in: query
      required: false
      schema: { type: string }
  requestBodies:
    GenericJson:
      required: false
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    Login:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [username, password]
            properties:
              username: { type: string }
              password: { type: string, format: password }
              sessionCookieConsent:
                type: boolean
                description: Must be true before the API issues the secure refresh-session cookie.
    AuthRegister:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
            properties:
              sessionCookieConsent:
                type: boolean
                description: Required when activating an invitation that immediately creates a secure session.
    TotpVerify:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [token]
            properties:
              token: { type: string }
              tempToken: { type: string }
    AnalyticsVisit:
      required: false
      content:
        application/json:
          schema:
            type: object
            properties:
              path: { type: string, example: /calendar }
              fullPath: { type: string }
              title: { type: string }
              referrer: { type: string }
              locale: { type: string, example: en-CA }
              timeZone: { type: string, example: America/Toronto }
    SiteConfigToken:
      required: false
      content:
        application/json:
          schema:
            type: object
            properties:
              configToken: { type: string }
    UploadUrl:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              filename: { type: string }
              contentType: { type: string }
              size: { type: integer, minimum: 0 }
              uploadSource:
                type: string
                enum:
                  [
                    retirementMessage,
                    lastPostMessage,
                    event,
                    mediaManager,
                    pageBuilder,
                    migration,
                    directUpload,
                    legacyStorage,
                    unknown,
                  ]
              uploadContext: { type: string }
              sourceId: { type: string }
              sourceModel: { type: string }
              sourceField: { type: string }
              sourceUrl: { type: string }
              sourceSlug: { type: string }
              sourceName: { type: string }
    Banner:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/BannerInput"
    Role:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RoleInput"
    BuiltInRole:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/BuiltInRoleInput"
    Page:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    PageStatus:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [status]
            properties:
              status:
                type: string
                enum: [draft, published, archived]
    NavigationItem:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    Event:
      required: true
      content:
        application/json:
          schema:
            type: object
            description: Event descriptions and registration instructions are limited to 10,000 characters per language.
            additionalProperties: true
    RetirementMessage:
      description: >-
        The `submitter` object accepts `relationship`; submitter contact details are derived from the authenticated profile on creation and preserved on updates. Message text is limited to 10,000 characters. A complete optional `certificateRequest` creates a separate pending certificate-request record. The retirement member's rank and MOSID/role are copied from `retiree`, and every certificate field is required except `member.ceBranchEnrollmentDate`. The message enters the review queue by default. Set `publishNow: true` only when the authenticated user has `canBypassReviewStages`.
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              photoUrl:
                type: string
                format: uri
                description: Full original photo URL for the retirement message.
              photoDisplayUrl:
                type: string
                format: uri
                description: Optional 4:3 display derivative returned by POST /api/upload for card layouts.
              publishNow:
                type: boolean
                description: Publish immediately when the authenticated user has `canBypassReviewStages`.
              certificateRequest:
                type: object
                required: [member, familyMembers, mailingAddress]
                properties:
                  member:
                    type: object
                    required:
                      [
                        fullName,
                        rankLanguage,
                        decorations,
                        lastUnit,
                        cafEnrollmentDate,
                        releaseDate,
                        neededByDate,
                        dwdParadeRequested,
                      ]
                    properties:
                      fullName: { type: string }
                      rankLanguage: { type: string, enum: [en, fr] }
                      decorations:
                        type: array
                        minItems: 1
                        items: { type: string }
                      lastUnit: { type: string }
                      cafEnrollmentDate: { type: string, format: date }
                      releaseDate: { type: string, format: date }
                      ceBranchEnrollmentDate: { type: string, format: date }
                      neededByDate: { type: string, format: date }
                      dwdParadeRequested: { type: boolean }
                  familyMembers:
                    type: array
                    items:
                      type: object
                      required: [relationship, fullName]
                      properties:
                        relationship: { type: string }
                        relationshipOther: { type: string }
                        fullName: { type: string }
                  mailingAddress:
                    type: object
                    required:
                      [line1, line2, city, province, postalCode, country]
                    properties:
                      line1: { type: string }
                      line2: { type: string }
                      city: { type: string }
                      province: { type: string }
                      postalCode: { type: string }
                      country: { type: string }
            additionalProperties: true
    ReviewDecision:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              decision:
                type: string
                enum: [publish, reject, approve]
              status:
                type: string
              reason:
                type: string
    EventReviewContent:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [language, content]
            properties:
              language:
                type: string
                enum: [en, fr]
              content:
                type: object
                required: [title, location, description, registration]
                properties:
                  title:
                    type: string
                    maxLength: 500
                  location:
                    type: string
                    maxLength: 500
                  description:
                    type: string
                    maxLength: 10000
                  registration:
                    type: string
                    maxLength: 10000
    RetirementReviewContent:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [language, message]
            properties:
              language:
                type: string
                enum: [en, fr]
              message:
                type: string
                minLength: 100
                maxLength: 10000
    LastPostSubmission:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              [
                deceased,
                messageLanguage,
                message,
                publicationPermissionConfirmed,
              ]
            properties:
              deceased:
                type: object
                required: [fullRank, firstName, surname]
                properties:
                  fullRank:
                    type: string
                    maxLength: 80
                  firstName:
                    type: string
                    maxLength: 80
                  surname:
                    type: string
                    maxLength: 80
                  postNominal:
                    type: string
                    maxLength: 120
              messageLanguage:
                type: string
                enum: [en, fr]
              message:
                type: string
                minLength: 1
                maxLength: 10000
              imageUrl:
                type: string
                format: uri
              imageDisplayUrl:
                type: string
                format: uri
                description: Optional 4:3 display derivative returned by POST /api/upload; imageUrl remains the full original.
              publicationPermissionConfirmed:
                type: boolean
                description: Must be true to confirm chain-of-command consent.
              publishNow:
                type: boolean
                description: Allowed only with canReviewAndPublish.
    LastPostReviewContent:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [language, message]
            properties:
              language:
                type: string
                enum: [en, fr]
              message:
                type: string
                maxLength: 10000
    TranslationUpdate:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              en: { type: string }
              fr: { type: string }
  responses:
    Error:
      description: Error response.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    RateLimited:
      description: Too many requests. The `Retry-After` header states when to retry.
      headers:
        Retry-After:
          schema: { type: integer, minimum: 1 }
          description: Seconds until the request may be retried.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Message:
      description: Message response.
      content:
        application/json:
          schema:
            type: object
            properties:
              message: { type: string }
  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error: { type: string }
    VersionResponse:
      type: object
      properties:
        commit: { type: string }
        shortCommit: { type: string }
    PlausibleConfig:
      type: object
      required: [enabled]
      properties:
        enabled: { type: boolean }
        domain: { type: string }
        endpoint:
          type: string
          format: uri
    LocalizedText:
      type: object
      properties:
        en: { type: string }
        fr: { type: string }
    NewsArticleInput:
      type: object
      required: [title, content]
      properties:
        title:
          $ref: "#/components/schemas/LocalizedText"
          description: English and French titles are both required; each is at most 240 characters.
        content:
          $ref: "#/components/schemas/LocalizedText"
          description: English and French content are both required; each is at most 20,000 characters.
        imageUrl:
          type: string
          format: uri
          description: Optional uploaded image URL. When omitted, the canonical CMCEN crest is used.
        imageDisplayUrl:
          type: string
          format: uri
          description: Optional server-generated 16:9 crop used by news cards and the homepage rail.
        status:
          type: string
          enum: [published, draft]
          default: published
    BannerInput:
      type: object
      properties:
        title: { type: string, maxLength: 120 }
        text:
          $ref: "#/components/schemas/LocalizedText"
          description: Localized banner text. HTTP and HTTPS URLs in the text render as clickable links.
        color:
          type: string
          pattern: "^#[0-9a-fA-F]{6}$"
        textColor:
          type: string
          pattern: "^#[0-9a-fA-F]{6}$"
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        countdownAt:
          type: string
          format: date-time
          nullable: true
        placement:
          type: string
          enum: [global, home]
        enabled:
          type: boolean
        order:
          type: number
    RoleInput:
      type: object
      properties:
        name: { type: string }
        slug: { type: string }
        color:
          type: string
          pattern: "^#[0-9a-fA-F]{6}$"
        description: { type: string }
        permissions:
          type: array
          items: { type: string }
    BuiltInRoleInput:
      type: object
      required: [role]
      properties:
        role:
          type: string
          enum:
            [
              ghost,
              subscriber,
              internal_beta,
              contributor,
              author,
              editor,
              administrator,
              developer,
            ]
